type Note @model {
  id: ID!
  name: String!
  description: String
}

type spotlightVideo @model  {
  id:ID!
  link: String
  name: String
  videoId: String
  beginAt: String  @function(name: "videoTimerFunction-${env}")
  isFinishedPlaying: Boolean
  duration: Int 
}